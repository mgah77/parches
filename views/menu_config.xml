<odoo>
<data>

    <record id="account.menu_finance_configuration" model="ir.ui.menu"> <!-- facturando/configuracion -->
        <field name="groups_id" eval="[(6, 0, [ref('parches.group_config')])]"/>
    </record>
    <record id="account.menu_finance_reports" model="ir.ui.menu"> <!-- facturando/informes -->
        <field name="groups_id" eval="[(6, 0, [ref('parches.group_config')])]"/>
    </record>

    <record id="stock.menu_stock_config_settings" model="ir.ui.menu"> <!-- inventario/configuracion -->
        <field name="groups_id" eval="[(6, 0, [ref('parches.group_config')])]"/>
    </record>
    <record id="contacts.res_partner_menu_config" model="ir.ui.menu"> <!-- contactos/configuracion -->
        <field name="groups_id" eval="[(6, 0, [ref('parches.group_config')])]"/>
    </record>
    <record id="sale.menu_sale_config" model="ir.ui.menu"> <!-- ventas/configuracion -->
        <field name="groups_id" eval="[(6, 0, [ref('parches.group_config')])]"/>
    </record>

    <!-- restringidos -->

    <record id="stock.menu_warehouse_report" model="ir.ui.menu"> <!-- inventario/informes -->
        <field name="groups_id" eval="[(6, 0, [ref('parches.group_restric')])]"/>
    </record>

    <record id="account.menu_finance_payables" model="ir.ui.menu"> <!-- facturando/proveedores -->
        <field name="groups_id" eval="[(6, 0, [ref('parches.group_restric')])]"/>
    </record>
    
    <record id="l10_cl_fe.menu_sii_exchange" model="ir.ui.menu"> <!-- facturando/xml intercambio -->
        <field name="groups_id" eval="[(6, 0, [ref('parches.group_restric')])]"/>
    </record>

    <record id="stock_forecast.menu_stock_forecast" model="ir.ui.menu"> <!-- inventario/analisis -->
        <field name="groups_id" eval="[(6, 0, [ref('parches.group_restric')])]"/>
    </record>
        

 
</data>
</odoo>